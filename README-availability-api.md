# Chris the Barber Availability API

Read-only API Gateway and Lambda package. Final customer details, deposits, and confirmation remain on Setmore.

Install files into `christhebarber-portal/backend/availability`, `infrastructure`, and `scripts`, then run the deployment script.
